/*game = {
    initGame(container) {
        container = document.body;
        container.innerHTML = `
      <div class="game">
        <canvas id="canvas1"></canvas>
        <canvas id="canvas2"></canvas>
        <canvas id="canvas3"></canvas>
        <div class="buttons">
          <button class="btn" onclick="SwitchToMainPage()">Main Page</button>
        </div>
      </div>`;
    },
    startGame() {
        this.initGame();
        this.initScripts('game/setup.js');
        this.initScripts('game/frog.js');
        this.initScripts('game/objects.js');
        this.initScripts('game/utilities.js');
    },
    initScripts(url) {
        let script = document.createElement('script');
        script.src = url;
        document.body.appendChild(script);
    }
}
game.startGame();*/


